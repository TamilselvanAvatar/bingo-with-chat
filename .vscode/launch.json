{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "DEBUG WEB SOCKET SERVER",
            "program": "${workspaceFolder}/src/server/socket-server.js",
            "env": {
                "PORT": "8080"
            },
            "skipFiles": [
                "<node_internals>/**"
            ]
        }
    ]
}